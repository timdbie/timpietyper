import React from "react";
import Bar from '../Bar/Bar'

const Main: React.FC = () => (
    <div className="flex flex-1 justify-center items-center">
        <Bar />
    </div>
);

export default Main;